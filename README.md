# 💡 Задание: Класс `BankAccount`

Создайте класс `BankAccount`, который представляет банковский счёт.

## Требования к классу

- У класса **обязательно должны быть** следующие методы:
  - `__init__(self, account_number, balance)` — конструктор;
  - `add(self, amount)` - пополнение счёта;
  - `withdraw(self, amount)` - снятие средств;
  - `status(self)` - вывод текущего баланса в формате `"Баланс счёта: {balance} единиц"`.
- Внутри класса должны присутствовать **внутренние переменные (атрибуты экземпляра)**:
  - `account_number` - номер счёта (строка или число, значение не проверяется в выводе);
  - `balance` - текущий баланс (целое число).
- Метод `status()` **должен вызываться в самом конце конструктора `__init__`**, чтобы сразу после создания объекта отображался начальный баланс.
- Метод `add(amount)`:
  - Принимает положительное число `amount`;
  - Увеличивает `balance` на `amount`;
  - Выводит сообщение: `"На счет зачислено: amount единиц"`.
- Метод `withdraw(amount)`:
  - Принимает **отрицательное** число `amount` (например, `-300`);
  - Фактическая сумма снятия = `abs(amount)`;
  - Если `abs(amount) <= balance`:
    - Уменьшает `balance` на `abs(amount)`;
    - Выводит: `"Со счета снято: abs(amount) единиц"`;
  - Иначе:
    - Выводит: `"Недостаточно средств на счете"`;
    - Баланс **не изменяется**.

> Все выводы должны выполняться с помощью функции `print()` и строго соответствовать формату из примеров.

## Примеры использования

### Входные и выходные данные

| Входные данные (`balance`, `operation`) | Ожидаемый вывод |
|-----------------------------------------|-----------------|
| `1000`<br>`200`                         | `Баланс счёта: 1000 единиц`<br>`На счет зачислено: 200 единиц`<br>`Баланс счёта: 1200 единиц` |
| `1000`<br>`-300`                        | `Баланс счёта: 1000 единиц`<br>`Со счета снято: 300 единиц`<br>`Баланс счёта: 700 единиц` |
| `1000`<br>`-1100`                       | `Баланс счёта: 1000 единиц`<br>`Недостаточно средств на счете`<br>`Баланс счёта: 1000 единиц` |

> **Пояснение**:  
> - Положительное значение → вызов `add(amount)`  
> - Отрицательное значение → вызов `withdraw(amount)`

## Инструкция по проверке

1. Создайте экземпляр: `acc = BankAccount("12345", initial_balance)`.
2. При создании объекта автоматически выводится стартовый баланс (вызов `status()` из `__init__`).
3. В зависимости от знака второго числа вызовите нужный метод.
4. Сравните весь вывод программы с ожидаемым.

Убедитесь, что:
- Все четыре метода (`__init__`, `add`, `withdraw`, `status`) присутствуют;
- Атрибуты `account_number` и `balance` инициализируются в `__init__`;
- `status()` вызывается в `__init__`;
- Нет дублирования логики вывода баланса вне метода `status()`.

Ваш класс проверяется этим кодом (в файле task.py этого кода быть не должно):
```
start = int(input())
money_move = int(input())
acc = BankAccount("123456578", start)
if money_move >= 0:
    acc.add(money_move)
else:
    acc.withdraw(abs(money_move))
acc.status()
```

